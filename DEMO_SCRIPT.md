# LATCH two-minute demo rehearsal

Status: offline implementation verified; live inference and real provider persistence remain unverified until local credentials are configured. Record real screenshots only after those checks. Do not narrate a successful save without a returned provider ID and read-back.

0:00–0:20 — Show the sample team workspace and date/timezone. Explain that promises get buried in a chat.
0:20–0:45 — Click Catch commitments. Show four model-extracted commitments and the separate voice suggestion. Open evidence for Marcus's slides.
0:45–1:05 — Follow the slides → rehearsal arrow labelled “must finish before”. Ben has no clock deadline. Deadlines use Kuala Lumpur time.
1:05–1:35 — Review a commitment, inspect exact fields and workspace identity, then approve in the browser. Show the real Ambiguous record ID. Refresh and retrieve that same ID.
1:35–1:50 — Show that declining another proposal does not create a task. Explain that chat can open review but cannot approve.
1:50–2:00 — Edit a sample deadline and re-extract, showing that detection changes while saved tasks remain intact.

Fallback if credentials fail: show the honest error/retry state and state that live verification is pending. Offline tests demonstrate validation boundaries, not live model quality or provider availability.
